const Glow = () => {
    return (
        <div
            className="absolute inset-0 bg-gradient-to-b from-transparent via-sky/20 to-transparent blur-3xl -z-10"
            aria-hidden="true"
        />
    );
};

export default Glow;
